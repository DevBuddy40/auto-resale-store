{
  "name": "Auto-Resale Store",
  // keep using the default image; no Docker in Docker needed
  "postStartCommand": "PORT=5680 N8N_BASIC_AUTH_ACTIVE=false npx n8n &",
  "customizations": {
    "vscode": {
      "settings": { "terminal.integrated.defaultProfile.linux": "bash" }
    }
  }
}
